const express = require('express');
const app = express();
const PORT = process.env.PORT || 3000;

// ===========================================================
// 1. 기본 페이지 (서버가 살아있는지 확인용)
// 브라우저로 이 주소에 접속하면 이 메시지가 보여야 정상
// ===========================================================
app.get('/', (req, res) => {
  res.send('색상칩 앱 서버가 정상적으로 동작 중입니다.');
});

// ===========================================================
// 2. 카페24 OAuth 콜백 주소
// 카페24 관리자가 앱을 설치하면, 카페24가 이 주소로
// "인증코드(code)"와 "쇼핑몰 정보(mall_id)"를 보내줌
// ===========================================================
app.get('/auth/callback', (req, res) => {
  const { code, state, mall_id } = req.query;

  console.log('=== 카페24에서 도착한 정보 ===');
  console.log('인증코드(code):', code);
  console.log('쇼핑몰 ID(mall_id):', mall_id);

  // TODO (다음 단계에서 추가할 작업):
  // 이 code를 카페24 서버에 다시 보내서
  // "Access Token"이라는 진짜 출입증으로 교환하는 코드를 여기에 작성

  res.send('인증이 도착했습니다! 이 창은 닫으셔도 됩니다.');
});

app.listen(PORT, () => {
  console.log(`서버가 ${PORT}번 포트에서 실행 중입니다.`);
});
